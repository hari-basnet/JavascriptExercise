const fullName = (firstName, lastName) => {
    return firstName + " " + lastName;
}

console.log(fullName("hari", "Basnet"));


const volumeOfRectangle = (length, breadth, height) =>{

    let area = length * breadth * height ;
    return area;
}

console.log(volumeOfRectangle(12,2,2));

